[project]
name = "littlex_auth"
version = "0.1.0"
description = "Episode 7 — authentication and multi-user LittleX"
entry-point = "main.jac"
kind = "cli"

[dependencies]

[dev-dependencies]
watchdog = ">=3.0.0"
